  location ~ /.*\.php(/.*)?$ {
    client_max_body_size 16M;
    include snippets/fastcgi-php.conf;
    fastcgi_pass unix:/run/php/php7.4-fpm.sock;
    fastcgi_param CONFIG_DIR /var/www/html/local;
    # fastcgi_read_timeout value goes here, if needed.
  }
